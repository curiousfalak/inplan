package com.example.inplan.ui.theme

import androidx.compose.ui.graphics.Color

/* --------------------------------------------------
   INPLAN BRAND COLORS
-------------------------------------------------- */

val InPlanBlue = Color(0xFF234EEF)
val InPlanBlueDark = Color(0xFF0B37C7)
val InPlanBlueLight = Color(0xFF4F7DFF)

val InPlanWhite = Color(0xFFFFFFFF)

/* --------------------------------------------------
   BACKGROUNDS
-------------------------------------------------- */

val BgBase = Color(0xFFFFFFFF) // was 0xFF1957EE (bright blue) — that's what tinted the whole screen, bottom bar, and card notches blue
val BgScaffold = Color(0xFFF2F5FA)

val CardSurface = Color(0xFFFFFFFF)
val CardBorder = Color(0xFFE7ECF4)

/* --------------------------------------------------
   TEXT
-------------------------------------------------- */

val TextPrimary = Color(0xFF111827)
val TextSecondary = Color(0xFF6B7280)

/* --------------------------------------------------
   SEMANTIC COLORS
-------------------------------------------------- */

val SuccessGreen = Color(0xFF22C55E)

val OwesOrange = Color(0xFFFF8A3D)

val ConvertedAmber = Color(0xFFD4A72C)

val ErrorRed = Color(0xFFD8452E)

/* --------------------------------------------------
   BACKWARD COMPATIBILITY
-------------------------------------------------- */

// Existing code may still use these names

val GetsBackGreen = SuccessGreen

val OwesRed = OwesOrange

val CancelledOrange = OwesOrange